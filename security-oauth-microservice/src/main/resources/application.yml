banner:
  location: classpath:static/banner.txt
        
spring:
  application:
     name: security-oauth-microservice  # Service registers under this name
  
logging:
  level:
    org.springframework:
      security: DEBUG
      
server:  
  context-path: /auth-server
